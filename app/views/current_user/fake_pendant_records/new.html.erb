<%= simple_form_for @pendant_record, url: my_fake_pendant_records_path, remote: true do |f| %>
  <%= f.input :pendant_id, collection: @pendants, label_method: ->(pendant) { "#{pendant.name} - #{pendant.artist_name}"} %>

  <%= f.input :worn_on %>

  <%= f.button :submit %>
<% end %>
