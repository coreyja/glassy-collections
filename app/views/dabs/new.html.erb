<%= simple_form_for @dab do |f| %>
  <%= f.input :user_id, as: :hidden %>
  <%= f.input :nail_id, collection: current_user.nails %>
  <%= f.input :milliseconds %>

  <%= f.submit :submit %>
<% end %>
